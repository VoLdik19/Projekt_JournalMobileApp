import { FilterPinnedPipe } from './filter-pinned.pipe';

describe('FilterPinnedPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPinnedPipe();
    expect(pipe).toBeTruthy();
  });
});
